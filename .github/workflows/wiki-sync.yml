name: Sync Wiki

on:
  push:
    branches: [main]
    paths:
      - "docs/wiki/**"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: wiki-sync
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: repo

      - name: Publish docs/wiki to GitHub Wiki
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          WIKI_URL="https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.wiki.git"
          WORK=/tmp/kyto-wiki-push
          rm -rf "$WORK"

          if git clone "$WIKI_URL" "$WORK" 2>/dev/null; then
            echo "Existing wiki cloned"
          else
            echo "Initializing new wiki repository"
            mkdir "$WORK"
            cd "$WORK"
            git init -b master
            git remote add origin "$WIKI_URL"
          fi

          rsync -av --delete --exclude README.md --exclude .git \
            "${{ github.workspace }}/repo/docs/wiki/" "$WORK/"
          cd "$WORK"
          git add -A
          if git diff --staged --quiet; then
            echo "Wiki already up to date"
            exit 0
          fi
          git commit -m "Sync wiki from docs/wiki"
          git push -u origin master
